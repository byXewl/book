运算符
```
like '12%' //任意位通配
like '12_'  //一位通配
and &&
or ||
between ... and ...
不等于!= <>
等于=
在之内 in(...)    a=. or a=.. or a=...
是null is null
否 ! not
```

执行顺序
```
from
where
group by
having
select
order by
limit
```